/**
 * @file le_Config.hpp
 * @brief Auto-generated configuration file for Logic Elements
 * 
 * This file is generated by CMake based on build options.
 * DO NOT EDIT THIS FILE DIRECTLY - Edit CMakeLists.txt instead!
 */

#pragma once

/**
 * Enables use of element constructors without use of the le_Engine factory
 */
#cmakedefine LE_ELEMENT_TEST_MODE

/**
 * Allow engine execution diagnostics
 * User must define le_Engine::GetTime()
 */
#cmakedefine LE_ENGINE_EXECUTION_DIAG

/**
 * Enables use of the analog elements in library
 */
#cmakedefine LE_ELEMENTS_ANALOG

#ifdef LE_ELEMENTS_ANALOG
/**
 * Enables use of the complex output of elements in library
 * NOTE: LE_ELEMENTS_ANALOG must be defined as well
 */
#cmakedefine LE_ELEMENTS_ANALOG_COMPLEX

/**
 * Enable use of the le_PID class
 * NOTE: LE_ELEMENTS_ANALOG must be defined as well
 */
#cmakedefine LE_ELEMENTS_PID

/**
 * Enables the use of the le_Math class
 */
#cmakedefine LE_ELEMENTS_MATH

#endif // LE_ELEMENTS_ANALOG

/**
 * Enable DNP3 session via le_DNP3Outstation
 */
#cmakedefine LE_DNP3

// Configuration constants
#define LE_ELEMENT_NAME_LENGTH 16
#define LE_ENGINE_NAME_LENGTH 32
#define LE_ELEMENT_ARGUMENT_LENGTH 64

// Version information
#define LE_VERSION_MAJOR @LE_VERSION_MAJOR@
#define LE_VERSION_MINOR @LE_VERSION_MINOR@
#define LE_VERSION_PATCH @LE_VERSION_PATCH@
#define LE_VERSION_STRING "@LE_VERSION_MAJOR@.@LE_VERSION_MINOR@.@LE_VERSION_PATCH@"

// ============================================================================
// Port Naming Configuration
// ============================================================================

/**
 * Default port name prefixes and separators for auto-generated port names.
 * These can be customized to change naming conventions across all elements.
 * 
 * Examples:
 *   Default:   "input_0", "output_0"
 *   Uppercase: "IN_0", "OUT_0"  (set PREFIX to "IN"/"OUT")
 *   Compact:   "in0", "out0"     (set SEPARATOR to "")
 *   Verbose:   "Input_0"         (set PREFIX to "Input")
 */
#define LE_PORT_INPUT_PREFIX "input"
#define LE_PORT_OUTPUT_PREFIX "output"
#define LE_PORT_SEPARATOR "_"
#define LE_PORT_MATH_VAR_PREFIX "x"
#define LE_PORT_SELECTOR_NAME "selector"

// Helper macros for consistent port naming
#define LE_PORT_INPUT_NAME(index) (LE_PORT_INPUT_PREFIX LE_PORT_SEPARATOR + std::to_string(index))
#define LE_PORT_OUTPUT_NAME(index) (LE_PORT_OUTPUT_PREFIX LE_PORT_SEPARATOR + std::to_string(index))
#define LE_PORT_INPUT_2D_NAME(set, signal) (LE_PORT_INPUT_PREFIX LE_PORT_SEPARATOR + std::to_string(set) + LE_PORT_SEPARATOR + std::to_string(signal))
#define LE_PORT_OUTPUT_2D_NAME(set, signal) (LE_PORT_OUTPUT_PREFIX LE_PORT_SEPARATOR + std::to_string(set) + LE_PORT_SEPARATOR + std::to_string(signal))
#define LE_PORT_MATH_VAR_NAME(index) (LE_PORT_MATH_VAR_PREFIX + std::to_string(index))